version: "3"

services:
  front:
    container_name: front
    build:
      context: ./front
      dockerfile: Dockerfile

    volumes:
      - ./front/src:/app/src
      - ./front/public:/app/public
    restart: always
    ports:
      - 3000:3000
